<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{{pageTitle}}</title>
    <link rel="stylesheet" href="/index.css" />
    {{#if productCSS}}
      <link rel="stylesheet" href="/product.css" />
    {{/if}}
    {{#if formCSS}}
      <link rel="stylesheet" href="/form.css" />
    {{/if}}
  </head>
  <body>
  <main>
    <header>
      <nav>
        <ul>
          <li><a className="{{#if activeProduct}}linkActive{{/if}}"href="/">Shop</a></li>
          <li><a className="{{#if activeForm}}linkActive{{/if}}" href="/admin/add-product">Add product</a></li>
        </ul>
      </nav>
    </header> 
    {{{body}}}
 </main>
 </body>
</html>